//C Program to Calculate Fahrenheit to Celsius

#include <stdio.h>

int main()
{
    printf("enter temperature in celsius: ");
    float temp;
    scanf("%f", &temp);
    printf("temperature in fahrenheit is %f",1.8*temp+32);
    return 0;
}
